<!DOCTYPE html>
<html lang="en">
    <%- include('partials/head') %>
<body>
    <h1>Details</h1>
        <% book.forEach(data => { %>
            <ul>
                <li><img src="<%= data.image_url %>"/></li>
                <li class="title"><%= data.title  %></li>
                <li class="author"><%= data.author %></li>
                <li><%= data.description %></li>
                <li><%= data.isbn %></li> 
            </ul>
            <% }) %>
            <a href="../books">Return to Book List</a>
</body>
</html>